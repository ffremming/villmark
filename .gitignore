# Unity source assets, not used by the web build
VoxBox/
.DS_Store

# Exported species models - uploaded to Hugging Face, never to git.
# The licence and the attribution do travel with the repo, so they are not lost.
models/*
!models/LICENSE
!models/NOTICE
!models/README.md
.venv/
__pycache__/
tools/sample_images/

# birder caches the downloaded weights here (MODELS_DIR is set in tools/common.py)
birder-cache/
tools/calibration/

# a test script made this symlink once. It breaks the GitHub Pages build.
__bilder
